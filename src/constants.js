const localStorageKey = "cartsItemKey";
const FICTION = 'fiction';
const TYPE_DISCOUNT = 0.15;
const POPUP = {
    title: "Deleted",
    message: "Item deleted from cart"
}

export { localStorageKey, FICTION, TYPE_DISCOUNT, POPUP };
